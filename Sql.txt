CREATE DATABASE schooldb;

USE schooldb;

CREATE TABLE Students12 (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    age INT,
    course VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO Students12 (name, age, course) VALUES ('Ranjitha', 21, 'CSE');
INSERT INTO students (name, age, course) VALUES ('Arun', 22, 'ECE'),('Divya', 20, 'EEE'),('Kiran', 23, 'IT');
SELECT * FROM students;
SELECT * FROM students WHERE id = 1;
SELECT * FROM students WHERE name LIKE '%ran%';
SELECT * FROM students ORDER BY age ASC;
UPDATE students SET age = 22, course = 'AIML'WHERE id = 1;
SET SQL_SAFE_UPDATES = 0;
UPDATE students SET course = 'CS' WHERE course = 'CSE';
DELETE FROM students WHERE id = 3;
TRUNCATE TABLE students;
SELECT COUNT(*) FROM students;
SELECT * FROM students ORDER BY age DESC LIMIT 1;
SELECT course, COUNT(*)  FROM students GROUP BY course;