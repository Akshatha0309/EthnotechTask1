
spring:
  datasource:
    url: jdbc:postgresql://db.zgquxetxwfwkkrniudez.supabase.co:5432/postgres
    username: postgres
    password: ospital@123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}